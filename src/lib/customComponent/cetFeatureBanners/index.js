import React from "react";

const cetFeatureBanners = (props) => (
    <div className="feature-banner">
        here is custom feature banner
        <div>{JSON.stringify(props)}</div>
    </div>
);

export { cetFeatureBanners };
